use bevy::prelude::*;

use crate::components::game::GameRule;

// fn game_over_check(rule: Res<GameRule>, )

pub struct GameManagePlugin;
impl Plugin for GameManagePlugin {
    fn build(&self, app: &mut App) {}
}
